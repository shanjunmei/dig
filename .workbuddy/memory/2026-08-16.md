# 2026-08-16

## DI 全面排查（shanjunmei/dig 消费代码审计）
- 对 `example/**` + `cmd/digen/di.go` 按 6 阶段流程（侦察→5 并行 Agent→对抗质疑→可达性→分级→可选修复）做了 digen 合规审计。
- 结论：消费代码整体合规，**无 P0/P1 真实缺陷**。
  - P2(1)：`cmd/digen/di.go:29-35` provider 闭包用 `log.Fatalln` 而非返回 error，绕过 dig error 路径；仅非法 `-alias` 触发，属最佳实践改进。
  - P3(3)：未使用 Provider 死重 — `cache/module.go:16`(*Cache[bool])、`app/di.go:31`("app-v2")、`app_debug/di.go:24`("app-debug")。
- **关键校正**：本仓库是 dig 框架本体（自导入 v1.0.15），不是下游消费者。
  - 真实约定：源文件 `di.go` 带 `//go:build digen`；`//go:generate go run .../cmd/digen` 写在**生成的** `dig_gen.go` 里。任务书假设的 `_dig.go` / `//go:generate digen` 在本仓库不存在。
  - `example/gen_failures/**` 是故意的失败夹具（gen_failures_test.go 覆盖），审计需排除。
- 验证：`go build ./...` 通过（EXIT=0）。未跑 `go generate`（会改写已提交 dig_gen.go）。
- 详细报告见 `DI-AUDIT-REPORT.md`（仓库根）。

## TDD 错误分支覆盖补全（逆向完善测试）
- 用户要求：用 TDD 思路，基于现有**所有错误分支**逆向建夹具 → 驱动验证 → 覆盖矩阵 + 用户视角评审。
- 枚举 `internal/` 约 60 个错误分支，与 28 个 `gen_failures` 夹具 + `internal` 单测比对 → 精确识别 **5 个无用户态夹具覆盖**的分支：
  - `buildtag.go:39`（缺 `//go:build digen`）、`extractor.go:291`（`does not contain dig.Module`）、
    `closure.go:1188/1191/1202`（入口函数返回值校验）。
- 新增 5 夹具：`example/gen_failures/{missing_build_tag,no_module,init_no_return,init_multi_return,init_bad_return}/di.go`，并在 `gen_failures_test.go` 的 `fixtures` map 登记期望子串。
- TDD 红→绿：初次 3 个转红（夹具 bug，非框架 bug）——
  - `init_no_return`/`init_bad_return` 误 import `context` 未使用 → 包编译失败早于校验；修复：删未用 import。
  - `no_module` 裸顶层 `dig.Provide` 被直接提取，先触发 unused-provider 分支；修复：改同包辅助函数 `helperWithoutModule() dig.Option` 由主 `dig.Module(...)` 包裹（命中 extractor.go:291 仅经 `extractOptionsFromFuncCall` 的路径）。
- 机制要点：harness 不传 `-tags digen` 跑 digen，但 digen 内部 `loader.go:35` 强制 `-tags digen` 加载包 → 缺约束夹具须**不带** `//go:build digen`，其余须带。
- 全量回归绿：`go build ./...` EXIT=0；`go test ./example/gen_failures/...` ok(33 夹具)；`internal/*` 与 `example/successtest/...` 均 ok。
- 报告：`TDD-COVERAGE-REPORT.md`（仓库根）。覆盖 100%。

## 生成器运行时正确性探针 + 一个真实缺陷
- 用户追问「依然没查到生成器本身的 bug？」。错误分支层无 bug；但问题是 successtest 只断言重生成的 `dig_gen.go` **能编译**，不验证**运行时语义正确**——最危险的「能编译但接线错」类 bug 全在盲区。
- 隔离探针（外部 temp module + replace=>../dig）：综合泛型 Provider(`*Repo[string]`/`*Repo[int]`) + 闭包 Invoke + `alias=obfuscated` + `inline=true`，真正 `go run` 校验运行时接线。
- 初版把类型/构造器/包级变量写在 `di.go`（带 `//go:build digen`）→ digen 自我保护式类型检查报「internal generator bug」并拒绝。实测这是**契约违反**（di.go 在正常构建被排除，其内符号对生成代码不可见），非生成器 bug；把符号挪到不带 tag 的 `types.go` 后，`full` 与 `obfuscated` 两种 alias 均生成成功且运行时 **PASS** → 证明生成器在硬路径上产出正确可运行代码。
- **真实缺陷（已修）**：`internal/generator/generator.go:156-158` 及 `buildGeneratorBugReport` 把「生成代码类型检查不过」一律谎称为「internal generator bug, 不是你的代码问题」。最常用真实成因是用户把符号定义在 di.go（digen tag 文件，构建期被排除）。已改为准确措辞：说明最可能因 di.go 内定义的符号在构建期不可见，提示挪到无 tag 文件/导入包，并保留「确属内部 bug」时的 issue 上报链接。已验证新消息出现且仍正确拒绝（digen_exit=1），全量回归绿（build/test EXIT=0）。
- 教训：本框架契约为「di.go 只放 dig.Build 接线，领域类型/构造器/包级变量必须定义在别的文件/包」；README/示例默认如此，但**该契约未被清晰报错**，导致易误判为工具 bug。

## 契约预检（early pre-check）+ golden-file 回归（用户最新两条请求）
- 用户要两件事：(A) 黄金文件 diff 测试（快照复杂 example 的生成结果，捕捉静默语义漂移）；(B) 对「用户违反 di.go 契约」在**生成前预检**清晰拦截，而非事后类型检查兜底+误导。
- **(B) 预检落地**：新增 `internal/extractor/contract.go` —— `checkContractVisibility()` 在 `BuildFinalNodes()` 提取之后、写文件之前运行。
  - `collectDigenDefinedMainObjects()`：扫 `pkgMap[mainPkgPath]`（digen 加载）的 `Types.Scope()`，取所有定义在 `//go:build digen` 文件中的包级对象（type/func/var/const）。
  - 对每 `item` 报告被引用且属该集合的主包符号，遍历：①`it.Expr` 标识符（闭包体/Supply 值）；②**非闭包构造器经 `it.Pkg.Types.Scope().Lookup(it.FuncName)` 解析**（关键坑：`handleProvide`/`handleInvoke` 不写 `it.Expr`，直接走 `it.Expr` 会漏掉非闭包 `dig.Provide(NewRepo)`）；③闭包签名+自由变量类型；④Supply 值类型；⑤合并参数类型。递归穿透指针/切片/映射/通道/泛型实参。
  - 锚点：`BuildFinalNodes()` 内 `buildFinalNodes()` 之后插入；返回清晰「digen contract violation」错误，写文件前中止。
  - **新夹具**：`example/gen_failures/contract_digen_symbol/di.go`（在 digen 文件内定义 `Repo`/`NewRepo` 并被接线引用）→ `fixtures` map 登记子串 `digen contract violation`。
- **(B) 兜底**：`generator.go` 的 `typeCheckGenerated`（TypeCheckNet）分类 "undefined: X"：若 X 属 digen 定义主包符号（`collectDigenDefinedMainNames`），输出契约消息而非 bug 上报。覆盖 IR-cache 命中（`-cache`）跳过 `BuildFinalNodes` 的路径。复用 `WriteGeneratedCode` 已有的 digen 加载 `pkg` 参数，未改其签名。
- **(A) golden-file 测试**：`example/golden/golden_test.go` —— 运行时发现 `example/*/dig_gen.go`（含 `//go:generate` 指令者，排除 `gen_failures`/`golden` 自身），解析其 `//go:generate` 还原精确 flags，重生成到临时 `-out`（不动已提交 golden），`normalize()` 剥离 `//go:generate` 元行后 diff。分歧即失败。
  - 覆盖 11 个复杂 example（app/app_basic/app_debug/app_edge/app_gen_test/app_runtime_err/app_xpkg_generic/closure_param/context_alias/shadow_err/shadow_freevar/supply_param），全部 **PASS**（当前生成器与已提交 golden 逐字节一致，证明无漂移）。
  - 已验证能抓漂移：临时给 `app_basic/dig_gen.go` 追加一行 → 测试 FAIL；`git checkout` 还原。
- **踩坑**：`checkContractVisibility` 初版依赖 `it.Expr!=nil`，非闭包 provide/invoke 的 `it.Expr` 为 nil → 预检静默不触发（被 `unused provider` 检查抢先）。改用包作用域 `Lookup(it.FuncName)` 解析构造器后修复。
- 全量回归绿：`go build ./...` EXIT=0；`go test ./internal/...`（extractor/generator/processor ok）；`go test ./example/gen_failures/...` ok(34 夹具)；`go test ./example/successtest/...` ok；`go test ./example/golden/...` ok(11)。
- 可复用沉淀：已更新 skill `digen-error-branch-tdd`（新增 Step 6 契约预检、Step 7 golden 测试，及 critical mechanic #4 it.Expr 坑、#5 golden normalize 坑）。

## 重构：消除 build 约束检查重复实现
- 问题（代码质量复查）：`extractor/buildtag.go:73` 的 `fileHasDigenConstraint` 与 `generator.go:318` 的 `genFileHasDigenConstraint` 逐行重复；其内层 `buildExprRequiresDigen`(extractor.go:249) 与 `buildExprRequiresDigenGen`(generator.go:342) 也重复（仅 `slices.Contains` vs 显式循环，行为等价）。
- 修复：新建叶子包 `internal/buildconstraint`（仅依赖 `go/ast`/`strings`），导出 `RequiresDigen(expr string) bool` 与 `FileHasDigenConstraint(f *ast.File) bool` 作为单一真源。
- 调用点全部收敛为 `buildconstraint.FileHasDigenConstraint`：generator.go:293、extractor/buildtag.go:34、extractor/contract.go:175（contract.go 初版漏改，复查补上）。
- 清理连带：删 extractor.go 的 `slices` 导入（仅被删函数用）、删 buildtag.go 的 `strings` 导入（仅被删函数用）。
- 新增 `internal/buildconstraint/buildconstraint_test.go`（RequiresDigen + FileHasDigenConstraint 单测，覆盖 `//go:build`/`+build`/约束在 package 之后的否定用例）。
- 全量回归绿：`go build ./...` EXIT=0；`go test ./internal/...`、`example/golden`、`example/successtest`、`example/gen_failures` 均 ok；buildconstraint 单测 PASS。gofmt -l 无差异。
