# 2026-08-18

## 文档优化（基于代码现状，覆盖度修复）
- 代码现状核对：`go.mod` 要求 **Go 1.25.0**（非 1.21+）；`cmd/digen` 含 5 个子命令 `init/check/graph/explain/completion`（post-v1.0.18，commit d5c97d1，不在 v1.0.18 tag）；示例实际为 12 个可运行 app + 34 个 `gen_failures` 负向样例。
- 修复的覆盖度/准确性问题：
  - README(中/英) 新增「CLI 命令」小节；核心特性补 ShadowGuard（与 GitHub Pages 对齐为 12 项）。
  - GitHub Pages `docs/index.html`：Go 版本 1.21+→1.25+（hero + 对比表）、核心能力 11→12、示例计数 41/29→12+34、新增 CLI 命令小节。
  - `prompts/system_prompt_dig(.md/_en.md)`：Go 1.21+→1.25+、补 `-typecheck/-cache/-cachedir`、补子命令、补 v1.0.16/17/18 特性与版本表。
  - `CHANGELOG(.md/_en.md)`：[Unreleased] 补 CLI 子命令条目（d5c97d1 引入，未随 v1.0.18 发布）。
- 未改动：仍保持「最新已发布 v1.0.18」口径，未擅自标记发布。审计/覆盖率报告（DI-AUDIT-REPORT、TDD-COVERAGE-REPORT、uiux-evaluation.html）未动。
- 改动文件 7 个，+86/−7；未提交（遵循用户「不擅自提交」规则）。

## 文档冗余清理（用户选定方案 B：主文档仅基于最新版本，历史差异分拆）
- README(中/英)：删除内联「Key/关键版本变更」历史块（15 行），改为单行 CHANGELOG 链接；剥离 CLI 标志表与核心特性中的 `(v1.0.x+)` /「自 v1.0.4 升级」等版本溯源注记；「LLM Agent Skills」列出分拆后的子技能。
- GitHub Pages `docs/index.html`：版本时间线仅保留 v1.0.18 条目（删 v1.0.17→v1.0.11 六条）+ 保留「完整变更日志」CTA；剥离 `-debug/-inline/-version` 行的版本注记；清理后修正了残留的空 `timeline-item` 节点。
- 技能分拆：新建 8 个文件——`dig-core(.md/_en.md)`、`dig-troubleshooting(.md/_en.md)`、`dig-migration(.md/_en.md)`、`dig-comparison(.md/_en.md)`；`system_prompt_dig(.md/_en.md)` 改写为薄 dispatcher（场景路由 + 通用约束 + 快速模板）。dig/Wire/Fx 对比矩阵唯一权威来源迁至 `dig-comparison*`。
- 单一事实源：历史版本差异现在只存在于 `CHANGELOG(.md/_en.md)` 与 `dig-migration*`；主文档/核心技能/对比技能均版本中立。
- 本轮改动文件（累计）：README.md、README_zh.md、docs/index.html、prompts/ 下 10 个文件；未提交（遵循「不擅自提交」）。

## 后续修正（去除「工作记录」口吻）
- 用户指出 README「LLM Agent Skills」中的「已分拆为…」属于工作记录，不应出现在项目文档中。
- 已调整：README(中/英) 改为中立的子技能列表（只说明每个文件是做什么的，不写「已拆分」）。
- 已调整：`prompts/system_prompt_dig(.md/_en.md)` 里的「已结构化分拆为」/「is decomposed into」改为「按场景拆分为」/「is organized into focused modules」，避免过程性语言。
- 工作记录本身保留在 `C:\Users\shanj\lab\dig\.workbuddy\memory\2026-08-18.md`（本文件），与项目文档分离。

## 结构归属收敛（用户拍板：skill 结构放主文档，不放 README）
- 评估结论：skill 内部结构（子模块清单 + 场景路由）属于 agent 运行期知识，应只在 skill 主文档 `system_prompt_dig(.md/_en.md)` 自描述；README 面向人类，只需一行中性指针，避免重复清单导致的漂移与「运行期机制泄漏进人类文档」。
- 已执行：README(中/英) 的「LLM Agent Skills」小节由「主 skill + 4 个子文件清单」收敛为单行指针（入口文件 + 覆盖主题，不列具体子文件名）。
- 约定（后续沿用）：新增/合并 skill 子文件时，只改 `system_prompt_dig*` 主文档，README 无需同步。

## 语言内联链接清理（中文 README 不应出现英文 skill/CHANGELOG 指针，反之亦然）
- 问题：用户发现中文 README「LLM 智能助手提示词」里冗余指向英文 skill `_en`；进一步审计后，版本注记行也在每个 README 里同时给出中/英 CHANGELOG 链接。
- 修复：
  - README_zh.md 中移除 `（英文：_en）`，只指向 `system_prompt_dig.md`。
  - README.md 的版本注记改为只链接 `CHANGELOG_en.md`；README_zh.md 只链接 `CHANGELOG.md`。
- 现状：跨语言链接只保留在顶部语言切换条（`[中文文档]` / `[English]`），正文内 skill/CHANGELOG 指针均为单语言。docs/index.html 为双语单页，保留内部语言切换，不受此规则约束。
- 结论：单语言 README 正文不应出现对侧语言文件的内联链接；语言切换统一走顶部切换条。

## 事实性内容审计（用户指出对比矩阵有过时信息）
- 逐项核对结果：
  - dig 侧全部准确：go.mod `go 1.25.0`；9 个 CLI 标志（out/unused/debug/alias/inline/version/typecheck/cache/cachedir）；5 个子命令（init/check/graph/explain/completion）；5 个核心 API（Build/Provide/Supply/Invoke/Module）；可运行示例 12 个（含 di.go 的目录，不含 gen_failures）；gen_failures 实际 **35** 个。
  - 第三方版本号并未过时：Wire 最新 **v0.7.0**（2025-08-22 发布，2025-08-25 归档）；Fx 最新稳定版 **v1.24.0**（2025-05）。
- 发现并修复的事实错误：
  1. Wire 维护口径被夸大：原写「已归档，上游不再接受任何新功能**或修复**」。实际 Google Wire 官方立场是「不再接受新功能，但仍接受 bug 修复」。已改 README(中/英) 的「Wire 特别说明」与对比矩阵「维护状态」单元格为「仅修 bug / bug-fix only」。
  2. 示例计数过时：gen_failures 实际 35，文档写 34。已改 `docs/index.html` 两处（示例区描述 + 失败示例标题）为 35。可运行示例仍为 12，无误。
- 未提交。

## 对比矩阵合理性修复（用户质疑：dig 依赖图可视化是否真的不支持 + 新特性未核实）
- 验证结论（核对源码 `cmd/digen/subcommands.go`）：
  - `digen graph` **确实输出依赖图**：`runGraph`（line 117）+ `renderMermaid`（line 139）打印 Mermaid `flowchart TD`，每个 package 一张图。→ dig **支持**依赖图可视化。
  - `digen explain <type>`（line 177）：解释某类型/提供者的解析路径（按名称或返回类型匹配），是 dig 独有能力。
  - `digen check`（line 88）：校验 DI 契约但**不写文件**，是比「生成即校验」更轻量的静态校验子命令。
- 发现并已修复的事实错误（直接印证用户质疑）：
  - README(中/英) 对比矩阵「依赖图可视化 (DOT)」行把 **dig 标成 ❌**（README.md:338 / README_zh.md:339）。这是错的——dig 通过 `digen graph`(Mermaid) 支持。已改为 `✅ digen graph（Mermaid）`，并把行名从 DOT 偏置改为中性「依赖图可视化」，Fx 仍 `✅ fx.DotGraph + fx.VisualizeError`（DOT）。
  - 同步新增「解析路径解释」行：dig `✅ digen explain` / Wire `❌` / Fx `❌`。
  - 同步把「不运行即可校验」行补上 dig 的 `digen check`（`✅ digen check` / 生成即校验）。
  - 上述三处改动在 README(中/英) 与 `prompts/dig-comparison(.md/_en.md)` 四份文件保持一致。
- 结构问题（待用户拍板，未擅自动）：README 仍内联一份 5 子表、约 50 行的丰富矩阵，而 `dig-comparison*` 是精简 12 行矩阵——两者已**漂移**（README 有 graph 行且曾标错，skill 副本无该行）。与「单一事实源」原则冲突。建议二选一：A) 删除 README 内联矩阵、改为链接 `dig-comparison*`；或 B) 明确 README 为「人类可读权威矩阵」、skill 副本保持同步精简。本轮未提交。

## 发版前全量文档核对（用户指令：更新所有文档、准备发版；不提交/不建 tag 分支）
- 核对方式：以源码为真相，逐项重新核实（用户「trust-but-verify」要求），不轻信上一轮记忆。
- 已核对的代码事实：go.mod `go 1.25.0`；9 个 CLI 标志（out/unused/debug/alias/inline/version/typecheck/cache/cachedir，main.go 确认）；5 个子命令（init/check/graph/explain/completion）；5 个核心 API（Build/Provide/Supply/Invoke/Module，di.go 确认）；可运行示例 **12** 个；`gen_failures` 失败样例 **34** 个（含 `gentest` 测试harness 共 35 个子目录，harness 不计入）。
- **纠正上一轮的错误**：本日志 line 45 写的「gen_failures 实际 35，文档写 34→改 35」是**错的**。上一轮用 `ls -d */|wc -l` 把 `gentest` 测试harness 子目录也算进了失败样例。真实失败样例数 = **34**。本轮已把 `docs/index.html` 两处（示例区描述 line 590、失败示例标题 line 611）由 35 改回 **34**。可运行示例 12 无误。
- 第三方版本复核（web 核验，pkg.go.dev / GitHub）：Wire 最新 **v0.7.0**（2025-08-22 发布，已归档）——矩阵正确；Fx 最新 **v1.24.0**（2025-05-13，pkg.go.dev 权威列表确认仍为最新）——矩阵正确，未过时。
- 发布纪律：全仓 grep 无 `v1.0.19`，CHANGELOG 仍 `## [Unreleased]`（无日期头），矩阵「最新版本」仍 v1.0.18，Pages 时间线仅 v1.0.18。未翻任何「已发布」标记。
- 结论：面向用户的文档（README 中/英、docs/index.html、CHANGELOG 中/英、prompts/*）现在与当前代码一致、可发；矩阵 graph/explain/check 行在 README 与 dig-comparison* 两处一致。「两矩阵结构收敛」仍待用户拍板（A/B），本轮未擅自动。本轮未提交。

## 发布执行（用户指令：对比矩阵先别动，发布，只改文档相关状态，用户自执行提交与发版）
- 用户明确授权「发布」，仅翻文档相关发布状态（版本号 v1.0.18→v1.0.19，日期 2026-08-18），不动对比矩阵结构（沿用此前「先别动了」拍板）。不提交、不建 tag/分支（用户自执行）。
- 已翻转的文档发布状态标记：
  - `CHANGELOG(.md/_en.md)`：`## [Unreleased]` → `## [v1.0.19] - 2026-08-18`（[Unreleased] 内容此前已完整）。
  - `README.md`：`> **Current version**: v1.0.18` → v1.0.19（line 14）；`| Latest version |` 表行 v1.0.18 → v1.0.19（line 356）。
  - `README_zh.md`：`> **当前版本**：v1.0.18` → v1.0.19（line 15）；`| 最新版本 |` 表行 v1.0.18 → v1.0.19（line 357）。
  - `docs/index.html`：版本徽章 `version-v1.0.18` → v1.0.19（line 59）；对比表「最新版本」单元格 v1.0.18 → v1.0.19（line 576）；版本时间线新增 v1.0.19 条目（2026-08-18，标题「生成期契约预检与诊断增强」）置于 v1.0.18 条目之上，原 v1.0.18 条目日期/内容保留（line 695 起）。
  - `prompts/system_prompt_dig.md`：`（当前 v1.0.18）` → `（当前 v1.0.19）`（line 12）。
  - `prompts/system_prompt_dig_en.md`：`(current v1.0.18)` → `(current v1.0.19)`（line 12）。
  - `prompts/dig-migration(.md/_en.md)`：版本变更简表新增 `| v1.0.19 | ... |` 一行（CLI 子命令 / 生成期契约预检 / 类型检查安全网分类 / buildconstraint 收敛 / golden 测试），置于 v1.0.18 行下（ZH line 23 / EN line 23）。
- 刻意保留为 v1.0.18 的位置（非发布状态、属历史/「自…起」口径）：CHANGELOG `v1.0.18 引入` 注记与 `## [v1.0.18] - 2026-08-15` 历史版本头；dig-migration `v1.0.18+` 特性口径行；`uiux-evaluation.html` 为 2026-08-16 评估 v1.0.18 构建的独立报告，不动。
- 全仓 grep 复核：v1.0.19 出现在上述 9 个位置；v1.0.18 剩余命中均为历史/「自…起」口径，符合预期。
- 状态：文档发布状态已全部翻到 v1.0.19，未提交、未建 tag/分支，等用户执行最后提交与发版。

## Release Note 草稿（用户问 CHANGELOG 能否直接当 release note，结论：宜重写一版用户向）
- 判断：CHANGELOG v1.0.19 条目偏「实现者视角」（含 internal/* 路径、IR 缓存缺口等），不宜原样当 GitHub Release 说明；应重排为用户向发布说明。
- 已新建 `RELEASE_NOTE_v1.0.19.md`（仓库根，草稿，未提交）：中/英对照，结构 = 新功能（digen 5 子命令）/ 诊断增强（契约预检 + 安全网分类）/ 升级注意（基本无破坏性，唯一变化是更早拦截本就非法的接线）/ 内部与质量（buildconstraint 收敛 + golden 测试）。事实全部源自 CHANGELOG，未新增。
- 未改 CHANGELOG、未翻发布标记；用户自决是否提交 / 贴到 GitHub Release。
- 用户后续要求：只要**英文版**，且像 CHANGELOG 一样建**累积式完整 release log 档案**。已新建 `RELEASE_NOTES.md`（英文、Keep-a-Changelog 格式、单文件承载 v1.0.0→v1.0.19 全部版本、最新在上）；v1.0.19 用用户向润色稿，v1.0.0–v1.0.18 由 CHANGELOG_en.md 事实底稿整理为用户向条目（去除内部 commit hash 与最底层实现路径噪声、保留行为实质）。已删除此前那份中英混杂的单版草稿 `RELEASE_NOTE_v1.0.19.md`。未提交。

## README 加入 GitHub Pages 文档站点链接（含语言深链）
- 评估结论（已与用户确认）：可加，但定位为「补充链接」而非「下沉能力」；README 须自包含决策关键信息（对比矩阵/快速开始/安装），Pages 只承载增强。合规要点：`*.github.io` 在华 intermittent 受阻（README 不能只留「详见站点」）；Page 无第三方追踪（已 grep 确认）；Page 页脚已有 MIT 许可（合规满足）；链接目标已实测在线且为 v1.0.19 最新内容。
- 落地改动（用户授权「落地」）：
  - `README.md:9` 语言切换条加 `[Documentation site](https://shanjunmei.github.io/dig/?lang=en)`。
  - `README_zh.md:10` 语言切换条加 `[文档站点](https://shanjunmei.github.io/dig/?lang=zh)`。
  - `docs/assets/js/main.js` 的 `getLang()` 新增 `?lang=zh|en` 参数优先逻辑：深链指定语言并持久化到 localStorage，使中文 README 点过去默认中文、英文 README 默认英文（此前默认中文，无法满足对应语言）。`node --check` 语法通过。
- 未提交；README 内决策关键信息未删减。
